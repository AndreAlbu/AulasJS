const frutas = ["Pera", "Uva", "Banana", "Goiaba"];

// for (let index in frutas) {
//   console.log(`Indice ${index} -> ${frutas[index]}`);
// }

const pessoas = {
  nome: "Andre",
  sobrenome: "Luis",
  idade: 25,
};

for (let chave in pessoas) {
  console.log(pessoas[chave]);
}
