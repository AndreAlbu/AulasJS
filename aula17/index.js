function soma(x, y){

    const soma = x + y;

    return soma;
}

console.log(soma(20, 3));

const raiz = function (n) {
    return n ** 0.5;
};

console.log(raiz(81));

const soma2 = (n, m) => {
    return n + m;
}

console.log(soma2(5, 6));